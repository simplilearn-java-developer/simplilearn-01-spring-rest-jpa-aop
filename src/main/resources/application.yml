
# Tomcat Server
server:
  port: 8080
  error.include-stacktrace: always

# Logger  
logging.level:
  web: DEBUG
  '[com.simplilearn.spring]': DEBUG

spring:
# Database Settings
  datasource:
    url: jdbc:mysql://localhost:3306/mydb
    username: root
    password: rootroot